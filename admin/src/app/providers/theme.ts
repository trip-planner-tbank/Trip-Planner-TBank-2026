import { defaultTheme } from "react-admin";

export const appTheme = {
  ...defaultTheme,
  palette: {
    ...defaultTheme.palette,
    primary: {
      main: "#2f54eb",
    },
    secondary: {
      main: "#13c2c2",
    },
  },
};
